<section class="ranking">
    <h2 class="sidebar-heading common-heading">月間いいね投稿ランキング</h2>
        <div class="sidebar-contents">
          <% @posts.each do |post| %>
          <% end %>

          <%= @all_ranks.each.with_index(1) do |post, i| %>
            第
            = i
            位
            = note.title
        </div>
  </section>